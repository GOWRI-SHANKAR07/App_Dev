import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: '#fff',
        height: '100%',
        width: '100%'
    },

    button: {
        padding: 10,
        borderRadius: 5,
        alignSelf: 'center',
        marginTop: 10,
        marginBottom: 30,
    },

    buttonText: {
        fontSize: 16,
    },
})

export { styles }
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        height: '100%',
        width: '100%',
        marginBottom: 20,
    },

    userCont: {
        borderWidth: 1,
        borderColor: '#000',
        padding: 10,
        margin: 20,
        borderRadius: 20
    },

    
})

export { styles }